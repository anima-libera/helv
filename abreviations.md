
Abreviation | Meaning
----------- | -------
`alloc`     | allocate, allocation
`cap`       | capacity
`char`      | character
`def`       | definition
`elem`      | element
`gs`        | growable string
`len`       | length
`i`         | index
`imm`       | immediate
`init`      | initialize, initialization
`instr`     | instruction
`pms`       | parsing mode set
`prog`      | program
`res`       | result
`rs`        | read-only stream
`src`       | source
`st`        | stack
`str`       | string
