
Abreviation | Meaning
----------- | -------
`alloc`     | allocate, allocation
`cap`       | capacity
`def`       | definition
`elem`      | element
`gs`        | growable string
`len`       | length
`init`      | initialize, initialization
`instr`     | instruction
`pms`       | parsing mode set
`prog`      | program
`res`       | result
`src`       | source
`st`        | stack
`str`       | string
